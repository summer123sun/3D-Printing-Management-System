# config

配置类（D）

- `CorsConfig.java` — 跨域配置（允许 `http://localhost:5173`）
- `MybatisPlusConfig.java` — MyBatis-Plus 分页插件
- `WebMvcConfig.java` — 注册拦截器 + 静态资源映射（`/uploads/` → 本地磁盘）
- `SwaggerConfig.java` — Knife4j 配置（接口文档）
