# resources

资源文件

- `application.yml` — 主配置（端口/数据库/JWT 密钥）
- `application-dev.yml` — 开发环境（覆盖主配置）
- `application-prod.yml` — 生产环境
- `mapper/` — MyBatis XML（如果不用 MyBatis-Plus）
- `static/uploads/` — 上传文件存储（开发环境）
